import React from 'react';
import './SearchPanel.css';

const SearchPanel = () => {
    return (
        <input
            className="search-panel form-control"
            type="text"
            placeholder="Įrašų paieška"
        />
    )
}

export default SearchPanel
